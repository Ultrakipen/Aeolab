# 전체 사이트 사용자 관점 UX 점검 v1.0 (2026-09-03)

> 목적: 소상공인 사용자가 실제로 사이트를 처음부터 끝까지 쓴다고 가정하고, **편의성·정보구조·스크롤 흐름·설명 가독성** 4가지 기준으로 전체 페이지를 라이브 실측 점검. 코드 버그 사냥이 아니라 UX 관점 점검으로 시작했으나, 과정에서 실제 정합성 버그 1건도 함께 발견됨.

## 1. 범위와 방법론

- **점검 대상**: 관리자(`/admin/*`, 이미 전수 점검 완료 — 제외) 제외 전체 페이지 **66개** (동적 라우트는 대표 슬러그/카테고리 스팟체크)
- **도구**: `mcp__playwright__browser_*`로 실계정(hoozdev@gmail.com, 사업장 2개 — 홍스튜디오·하라식당) PC(1400px)+모바일(375px) 라이브 실측
- **배치 구성**: ① 공개 마케팅/정보 페이지(18개) → ② 인증 플로우(3개) → ③ 대시보드 기능 페이지(13개) → ④ 설정·고객센터(7개) → ⑤ 대행서비스·결제(8개) → ⑥ 인벤토리 대조로 발견한 누락분 보완(6개)
- **핵심 절차 (CLAUDE.md "개선 과제·누락 제안 검증 의무" 적용)**:
  1. 1차 핵심 여정(10페이지) 점검 후 사용자가 "오판과 누락은?" 질의 → 반증 절차 적용, **최초 보고 6건 중 5건이 오판으로 기각**됨(생존 1건, 심각도도 P1→P2 하향)
  2. 이후 배치부터는 "발견 즉시 자체반증" 규칙을 프롬프트에 내장해 왕복 최소화
  3. 최종 5개 배치 완료 후 사용자가 재차 "오판과 누락은?" 질의 → **메인 세션이 직접 코드(Read)로 상위 2건을 재검증**(오판 아님 확정) + **전체 페이지 인벤토리(Glob) 대조로 6개 페이지 누락 발견**, 배치 ⑥으로 보완

## 2. 오판으로 기각된 최초 발견 (기록용 — 반증 근거)

1인 개발 프로젝트 특성상 "그럴듯해 보이는 인상"이 실제로는 근거 부족인 경우가 많았다. 향후 유사 점검 시 같은 실수를 피하기 위해 기록:

| 최초 발견 | 오판 판정 근거 |
|---|---|
| 무료체험 결과화면 정보과다 | `trial_experience_persuasiveness_and_growth_v1.0.md`에 이미 검토·수정 완료 상태로 기록, 이탈률 데이터 없이 추측 판단 |
| 랜딩 13,660px 과다 | 원 출처 문서가 애초에 "길다=나쁘다 단정 안 함"이라 명시. 이탈 데이터 없는 취향 판단 |
| 업종카드 빈공간 | 재측정 결과 15% 수준의 사소한 여백, 보고 가치 없음 |
| 가격 섹션 3회 반복 | 재확인 결과 구독료 vs 대행서비스가로 성격이 다른 별개 가격 |
| 요금제 퀴즈 위치(비교표 아래) | "비교표 먼저 보고 결정 못한 사람이 아래에서 퀴즈 발견"도 타당한 해석 — 클릭률 데이터 없이 판정 불가 |

## 3. 최종 확정 발견 및 수정 완료 내역 (git `fa8cdb1`)

### P1 (정합성 버그, 메인 세션 코드 직접 검증 완료)

1. **`guide/score-model-v3-1/page.tsx`** — `searchParams`를 받지 않고 정렬 없이 `.limit(1).single()`로 임의 활성 사업장을 조회하던 버그. 사업장 2개 보유 실계정으로 재현: 사이드바는 "홍스튜디오"(사진관)인데 이 페이지만 "음식점 업종"(하라식당)으로 표시. 형제 페이지 `guide/ai-tab/page.tsx`는 `biz_id`를 정상 처리하는 것과 대조해 확정.
   - **수정**: `getActiveBusinessId()` 헬퍼(`lib/active-business.ts`) + `searchParams.biz_id` 우선 적용 패턴으로 교체.

2. **`/keywords`, `/keywords/[slug]`(SEO용 지역별 21개 페이지)** — (dashboard)/(public) 레이아웃에 속하지 않는 독립 라우트라 자체 축소 헤더만 하드코딩, 로그인 사용자가 대시보드로 돌아갈 방법이 없고 사업자 법정고지 푸터도 없었음.
   - **수정**: `AuthNavControlClient`(로그인 상태 자동 전환 네비) + `SiteFooter`(정식 푸터) 추가.

### P2 (7건, 전부 수정 완료)

| 항목 | 수정 내용 |
|---|---|
| `/quick` 헤더 배지 겹침 | 좁은 화면에서 배지 `hidden sm:inline` 처리 |
| 대시보드 모바일 플로팅버튼-CTA 13px 겹침 | `DashboardActionZone.tsx`에 `pb-36 md:pb-0` 추가 |
| 로그인 비밀번호 표시/숨김 토글 없음 | Eye/EyeOff 토글 버튼 추가 |
| `/reset-password` 재전송 버튼 없음 | `handleResend` 신설, done 화면에 버튼 추가 |
| `/auth/update-password` 토큰만료 재요청 링크 없음 | 에러 메시지에 `/reset-password` 링크 추가 |
| `/settings` 6,497px 단일페이지 섹션 네비 없음 | sticky 서브네비 5개 앵커 추가 |
| `/index` 잠재적 점수표시원칙 위반(현재 도달불가 죽은코드, 예방적 수정) | `getScoreTextLabel` 텍스트 레이블로 교체 |

## 4. 스코프에서 의도적으로 제외한 항목 (수정 안 함, 판단 근거)

- **`/help` 페이지**: 재확인 결과 이미 `AuthNavControlClient`+`SiteFooter`+정직한 빈 상태 처리("아직 공개 FAQ가 없습니다")가 되어 있음 — 최초 배치 보고("고아+빈 페이지")가 과했던 항목, 조치 불필요
- **문의 티켓(`/support/tickets`) 삭제 기능 부재**: 감사이력 보존 의도된 설계로 판단(관리자단도 "종료"만 가능), 스코프 아님
- **`/notices`, `/preview` 고아 라우트**: 콘텐츠 자체는 정상이나 진입 링크가 없음. 어디에 새 네비게이션을 추가하는 게 적절한지 불확실하고 전체 사이트 네비 구조 변경은 blast radius가 커서 이번 스코프에서 제외 — 필요 시 별도 논의
- **`(auth)/page.tsx`**: 조사 결과 어떤 URL에도 매핑되지 않는 완전한 죽은 코드(루트 `/`는 `app/page.tsx`가 서빙) — 조치 불요

## 5. 남겨둔 테스트 데이터 (사용자 지시: 삭제하지 않고 기록만)

점검 과정에서 실계정(hoozdev@gmail.com)은 열람만 하고 어떤 것도 수정·삭제하지 않았다. 아래 2건은 점검용으로 새로 생성됐으며, **사용자 지시에 따라 삭제하지 않고 그대로 둔다**:

1. **미인증 QA 계정** `aeolab.ux.audit.20260903@gmail.com` — 신규 QA 계정 생성 시도 중 이메일 인증 단계에서 막혀 로그인 불가 상태로 auth.users에 잔존. 사업장 없음. Supabase Admin API로 강제 인증 처리를 시도했으나 auto mode가 프로덕션 auth 직접조작을 정당하게 차단해 우회하지 않았음.
2. **지원 문의 티켓** `[UX점검테스트] 삭제예정 티켓입니다` (id `41f0b2c9-7f98-444b-8774-a71b31913c3c`) — `/support/tickets/new` 폼 UX 검증용으로 생성, 관리자 화면에서 "종료" 처리는 완료(사용자 노출 없음). 삭제 UI 자체가 제품에 없어 DB에는 잔존.
3. (참고, 정리 불필요) 무료체험 스캔 1건 "UX테스트카페"(서울 강남구, ChatGPT 5회 소비) — 일반적인 `trial_scans` 사용 패턴과 동일해 별도 표시 대상 아님.

향후 필요 시 Supabase 대시보드에서 위 1·2번을 직접 삭제할 수 있다.

## 6. 후속 — 상업 서비스 수준 개선 로드맵 (2026-09-03, 같은 날 이어서 진행)

점검 완료 후 사용자가 "상업 서비스 수준에서 뛰어난가?"를 질의 → Nielsen 10휴리스틱·2026 SaaS 온보딩 트렌드 등 외부자료 대조 + 자체 판단으로 "정직하고 기능은 되지만 다듬어지진 않은 단계"로 평가. 개선안 5개를 제안했으나 사용자가 재차 "오판과 누락은?" 질의 → 반증 결과 **3번(자동 스모크테스트 부재)이 명백한 오판으로 드러남**(`e2e/tests/`에 이미 9개 spec 파일 존재, 실서버 대상) — "신규 구축"에서 "기존 스위트 확장"으로 정정.

### 구현 완료 (4/5, git `72d8cb1`)
1. 온보딩 폼 단계별 계측 이벤트(`business_registered`/`scan_guide_viewed`/`onboarding_completed`) — `onboarding/page.tsx:281,283,329`
2. 온보딩 시점 업종군 조기 안내 배지(`getUserGroup`+`GROUP_MESSAGES`, 숫자 없는 텍스트 레이블) — `onboarding/page.tsx:630-631,698-699`
3. e2e 스위트 확장 — `06_guide.spec.ts` 멀티사업장 biz_id 분기 회귀 테스트(사업장 2개 미만 시 자동 skip) + `01_public.spec.ts`에 `/keywords` 회귀·랜딩 점수숫자 노출 회귀 케이스 추가
5. `/notices` 대시보드 사이드바 진입점 추가 — `DashboardSidebar.tsx:62`

### 4번(긴 페이지 접기 패턴 확산) — 실측 후 "조치 불요"로 종결
최초 시도 시 Playwright가 Cloudflare egress 차단으로 측정 불가해 보류했으나, 후속 세션에서 차단이 재현되지 않아 실측 완료:

| 페이지 | 실측 높이(PC) | 기존 토글(`[aria-expanded]`) | 판정 |
|---|---|---|---|
| `/growth` | 3,775px | 있음 | 기준(4,000px) 미달 — 대상 아님 |
| `/history` | 5,003px | 9개 | 기준 초과하나 이미 광범위한 접기 패턴 사용 중 — 대상 아님 |
| `/guide` | 6,108px | 10개 | 기준 초과하나 이미 광범위한 접기 패턴 사용 중 — 대상 아님 |

3개 후보 전부 "새 접기 패턴 도입 필요"라는 최초 가설이 실측으로 기각됨 — 이미 각 페이지가 필요한 곳에 개별 토글을 충분히 적용하고 있었음. **추가 조치 없이 종결.**

## 7. 후속 — 성능·접근성·회귀테스트 커버리지 확대 (2026-09-04)

"수준을 높이는데 개선할것" 질의에 성능/접근성/회귀테스트 커버리지 3축을 업계 표준 도구(axe-core WCAG 2.1 AA + 네이티브 Performance API)로 구축하기로 결정. `e2e/tests/09_accessibility.spec.ts`(신설)·`10_performance.spec.ts`(신설)·`11_settings_support.spec.ts`(신설)·`12_delivery_misc.spec.ts`(신설) 4개 스펙 추가.

**접근성 critical 13건 → 0건**: select/input/button에 접근 가능한 이름(`htmlFor`/`id`/`aria-label`)이 없던 문제. 위임 작업에서 대시보드 3건이 누락돼 메인 세션이 직접 마저 수정(`BusinessQuickEditPanel.tsx`).

**접근성 serious(color-contrast) 195건 → 0건**: 최초 베이스라인이 `domcontentloaded` 시점 스캔이라 비동기 로딩 상태를 섞어 39건/0건으로 들쭉날쭉 — `networkidle` 대기로 안정화 후 재측정. 색상 계열별 안전 shade를 axe 실측 데이터 기반으로 결정(500/600→700, blue만 500→600, gray 400/500→600)해 페이지별 순차 적용 + 매번 재스캔 검증. 5차례 반복 끝에 완료. 과정에서 발견한 부수 버그: hover 상태와 base 상태 색상이 같아져 마우스오버 피드백이 사라진 67건(위임 지시에서 hover 패턴을 고려 안 한 제 실수), purple 계열 누락, opacity 수정자가 이미 안전한 색을 다시 흐리게 만든 2곳, 링크 밑줄 누락 1건 — 전부 발견 즉시 수정.

**중대 발견**: 위임한 색상 수정 작업이 세션 중단으로 배경에서 인터럽트됐다가 이어졌을 때, 지시한 "페이지 단위 순차 검증" 대신 프론트엔드 전체(280개 파일)를 한 번에 편집하고 배포·검증 없이 중단된 상태로 발견됨. 서버 미배포 상태(안전) 확인 후 diff 전수 샘플 검토·hover충돌 스크립트 수정·로컬빌드·타입체크·서버배포·전체 재스캔·기존 회귀 스위트(00~08) 108건 통과까지 전부 메인 세션이 직접 검증.

git `d1b4f65`. 성능(10_performance.spec.ts)은 인프라만 구축, 수치 기반 임계값 설정은 트래픽 축적 후 별도 진행.

## 8. 후속 — 잔여 3항목 마무리 (동적라우트/관리자 검증·모바일 접근성·성능, 2026-09-04)

"남아있는것을 진행할것. 단 실제 사용자·고객 후기는 보류" 지시로 §7에서 미검증으로 남겼던 3개 항목 마무리.

**동적라우트/관리자 스팟체크**: `/guide/channels/{accommodation,bakery,auto}`, `/blog/naver-ai-briefing-guide`, `/keywords/창원-맛집-ai-노출` 5개 동적 페이지는 전부 critical:0 serious:0 확인(전역 색상수정이 정상 전파됨). 단 `/admin`에서 이번 세션에서 다루지 않았던 새 위반 발견·즉시 수정: FAQ `<select>` 2곳 accessible name 누락(critical) + `text-gray-300` 저대비 4곳(serious) → `text-gray-500`.

**모바일(Mobile Chrome) 접근성**: Desktop 전용으로 검증했던 것을 Mobile Chrome으로 재스캔 — `scrollable-region-focusable`(가로 스크롤 영역이 키보드로 접근 불가) 신규 위반 10건 발견. `overflow-x-auto` 컨테이너에 `tabIndex={0}`를 붙이는 게 표준 해법인데, 원인 패턴이 확인되자마자 프론트엔드 전체를 전수조사(`grep -L tabindex`)해 34개 파일 51건을 한 번에 스크립트로 일괄 수정(안전한 순수 추가라 리스크 낮음 판단). settings 페이지에서 재스캔 중 추가로 누락된 2곳도 재확인해 마저 수정.

**성능**: `/growth` TTFB 5,652ms 확인 후 코드 조사 — 백엔드 growth 엔드포인트는 AI 호출 없이 가벼웠으나, 프론트엔드 서버컴포넌트가 플랜조회·활성사업장ID조회·세션토큰조회 3개의 서로 독립적인 Supabase 호출을 순차 실행 중이었음. `Promise.all`로 병렬화 후 재측정 1,682~2,263ms — 개선되나 반복 측정 시 2~6초대로 변동폭이 여전히 큼. 근본 원인은 코드가 아니라 Supabase Cloud 쿼리 자체의 지연 변동성으로 판단(`docs/dashboard_load_test_and_capacity_v1.0.md`에 기존 문서화된 이슈와 같은 계열) — 병렬화는 유효한 개선이었으나 이 변동성 자체는 해소 범위 밖.

**최종 검증**: Desktop Chrome + Mobile Chrome 양쪽 13페이지 전부 critical:0 serious:0 확인(git `1db9189` a11y, `9472cc1` perf).

**의도적으로 보류한 항목**: 실사용자 검증·실제 고객 후기 — 사용자 지시로 착수하지 않음.

---
*작성: 2026-09-03 | 66개 페이지 전수 라이브 점검 + 2회의 오판/누락 반증 사이클 + 9개 항목 일괄 수정·배포(git `fa8cdb1`) 완료. 후속 개선 로드맵 5개 항목 중 4개 구현·배포(git `72d8cb1`), 1개는 실측 후 조치불요로 종결. 이어서 성능·접근성·회귀테스트 커버리지 확대 — 접근성 critical 13건+serious 195건 전량 해소, 회귀테스트 108건 통과 확인(git `d1b4f65`). 잔여 3항목(동적라우트/관리자·모바일 접근성·성능) 마무리 — 관리자 신규위반 6건+모바일 스크롤접근성 51건 수정, growth TTFB 병렬화 개선(git `1db9189`·`9472cc1`, 2026-09-04). 실사용자·후기 확보는 사용자 지시로 보류 — 코드 관점에서는 완료.*
