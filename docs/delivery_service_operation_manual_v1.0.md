# 대행 서비스(등록 대행) 운영 매뉴얼 v1.0 — 내부용

> 작성 2026-07-17 | 대상: 운영자 본인(1인 운영) — 대행 서비스(스마트플레이스 등록 대행 등) 실제 작업 시 사용
> 근거: `backend/routers/delivery.py`(PACKAGES 단일 소스) + `frontend/app/(dashboard)/delivery/*` + `frontend/app/admin/delivery/*` 코드 직접 확인, `docs/agency_service_and_iboss_improvements_v1.0.md`(기획) + `docs/agency_pricing_strategy_v1.0.md`(가격 갱신 이력)
> **전제**: 2026-07-17 라이브 DB(`delivery_orders`) 직접 조회 결과 **실주문 0건**. 이 매뉴얼은 코드/기획 문서 기반 사전 체크리스트이며, **네이버 UI 조작이 필요한 항목은 미검증 상태로 남겨둠**(§9) — 지어내지 않고 첫 실제 건에서 캡처해 보완할 것.

---

## 0. 왜 필요한가

시스템(`delivery.py`)은 주문 상태 추적·결제·메시지·완료보고서 저장까지는 되어 있지만, **"실제로 무엇을 어떻게 작업하는지"는 코드 어디에도 없다** — 전부 운영자 머릿속에 있던 절차다. 관리자 완료 화면(`AdminDeliveryDetailClient.tsx:35-40`)의 체크리스트도 패키지 구분 없이 고정 4항목이라 패키지 01(등록 대행)에는 맞지 않는다(§5 참조). 실주문이 아직 0건이라 지금 만드는 이 문서는 "시범 없이 쓰는 최초 체크리스트"이며, 첫 실제 건을 마친 뒤 반드시 §9를 채워 정식 SOP로 갱신한다.

---

## 0-1. 완성도 자가진단 (2026-07-17, 사용자 질문에 대한 재점검)

이 문서만 보고 처음부터 끝까지 대행 작업을 수행할 수 있는지 재검토한 결과:

| 구간 | 상태 | 사유 |
|------|------|------|
| §2 상태 흐름·관리자 화면 조작 | ✅ 실행 가능 | 코드 직접 확인, 버튼·엔드포인트·전이 조건 전부 검증됨 |
| §3 접수~착수 체크리스트 | ✅ 실행 가능 | 부운영자 위임 "안내를 보내야 한다"는 사실까지는 검증됨(§9로 안내 문구 자체만 미완성) |
| §4.1 스마트플레이스 등록 대행 | ⚠️ 부분적 | "무엇을 해야 하는지" 목록은 맞지만, "네이버 화면에서 정확히 어디를 클릭하는지"는 없음 — WebSearch 2회 시도 후에도 공식 확인 불가, 지어내지 않고 §9로 이관 |
| §4.2 AI 검색 최적화(소개글·톡톡메뉴·후기답글) | ✅ 실행 가능 (수정 후) | 최초엔 API 참조가 실제로 부를 수 없는 도구였음 — §8을 "파일 직접 열어 복사"로 정정해 지금은 그대로 따라 하면 됨 |
| §5~§8 완료 처리·해제·도구 | ✅ 실행 가능 | 완료 체크리스트 불일치·부운영자 자동해제 없음 등 코드 검증된 함정 포함 |

**결론**: §4.2(AI 검색 최적화 패키지)와 완료/사후 처리 전체는 지금 바로 따라 할 수 있는 설명서 수준입니다. §4.1(등록 대행 패키지)의 "네이버 화면 조작" 부분만 실제 로그인 접근 없이는 검증할 방법이 없어 여전히 빈칸입니다.

> **⏸ 결정 보류 (2026-07-17)**: 빈칸을 채우는 3가지 방법(① 첫 실제 의뢰 시 캡처 ② 기존에 알고 있는 절차 문서화 ③ 테스트 계정으로 Playwright 직접 확인) 중 **아직 선택하지 않음**. 다음 세션에서 이어갈 때는 이 섹션과 §9를 먼저 확인하고, 결정된 방식으로만 §4.1을 채울 것 — 임의로 지어내거나 임의 방식을 골라 진행하지 말 것.

---

## 1. 서비스 3종 요약 (단일 소스: `backend/routers/delivery.py:51` PACKAGES, 가격은 `backend/config/prices.py DELIVERY_PRICES`)

| 코드 | 이름 | 가격 | 예상 작업시간 | features (계약상 산출물) |
|------|------|------|--------------|--------------------------|
| `smartplace_register` | 01 스마트플레이스 등록 대행 | 59,000원 | 5.2h | 스마트플레이스 신규 등록 / 기본정보·메뉴·키워드 최적화 / 대표 사진 구성 안내 |
| `ai_optimization` | 02 AI 검색 최적화 | 79,000원 | 6.0h | 소개글·톡톡채팅방 메뉴 최적화 / 후기 답글 10건 작성 / 핵심 키워드 보강 |
| `comprehensive` | 03 종합 풀패키지 | 129,000원 | 11.2h | 01 전체 + 02 전체 + 1:1 코칭 세션 + 30일 재진단 |

> features 목록은 고객에게 노출되는 계약 범위 그대로다 — **여기 없는 작업(예: 사진 직접 촬영·수상 이력 기재 등)은 범위 밖**이므로 임의로 하지도, 약속하지도 말 것.

---

## 2. 전체 처리 흐름 (상태 머신, `VALID_STATUSES`)

```
received(접수) → paid(결제완료) → in_progress(진행중) → completed(완료)
                                    ↳ rework(재작업, 최대 2회 권장)
결제 전: received → cancelled(취소, 결제 전만 가능)
결제 후: paid/in_progress/rework → refunded(환불, owner 전용, 토스 실환불 발생)
```

- 관리자 화면: `/admin/delivery`(목록) → `/admin/delivery/{id}`(상세, 메시지·상태변경·완료처리·납품파일)
- 카카오 알림톡 자동 발송 시점: `in_progress` 전환 시, `completed` 처리 시 (그 외 상태는 알림 없음)
- **"취소"와 "환불" 버튼을 혼동하지 말 것** — 결제 전 주문은 취소만 가능(환불 API 호출 안 됨), 결제 후 주문은 반드시 환불 처리(취소 버튼은 400 에러로 막힘, `delivery.py:807-812`)

---

## 3. 접수 ~ 착수 단계 체크리스트 (공통, 전 패키지)

1. `/admin/delivery`에서 신규 의뢰 확인 — `request_title`·`request_body`·첨부 자료 확인
2. 결제 완료(`paid`) 확인 후 "진행 시작" 클릭 → `in_progress` 전환 (카카오 알림 자동 발송)
3. **부운영자 위임 안내 발송** — ⚠️ 프론트 배너(`delivery/page.tsx:238-241`)가 "결제 완료 후 안내해 드립니다"라고 고객에게 약속하지만, **이 안내를 실제로 보내는 자동 기능은 코드에 없다.** 운영자가 카카오톡/메시지 기능(`/admin/delivery/{id}/messages`)으로 직접 안내를 보내야 함. 정확한 네이버 UI 경로는 §9 참조(현재 미검증).
4. 업종·주소·전화번호 등 기본 정보는 `businesses` 테이블에 이미 있는 값 재사용 (고객에게 중복 질문 금지)
5. 사업장이 **프랜차이즈**인 경우 AI 브리핑 관련 안내 문구는 제외 대상임을 인지(`score_engine.py` 프랜차이즈 제외 규칙, 등록 대행 자체는 무관)

---

## 4. 패키지별 실무 체크리스트

### 4.1 `smartplace_register` — 01 스마트플레이스 등록 대행 (5.2h)

- [ ] 스마트플레이스 신규 등록 신청 — **정확한 네이버 UI 절차 미검증, §9에서 첫 건 진행 시 캡처해 보완**
- [ ] 기본정보 입력 (영업시간·휴무일·전화번호·주소·부가정보)
- [ ] 메뉴/서비스 항목 등록 — 대량 등록은 `GET /api/tools/menu-template.xlsx`(메뉴 일괄 등록 엑셀 양식) 활용 가능
- [ ] 키워드 최적화 — 업종별 키워드는 `keyword_taxonomy` 기준 확인(`docs/naver_gpt_work_standard_v1.0.md` 필수 참조)
- [ ] 대표 사진 "구성 안내" — 계약 범위는 **촬영이 아니라 구성 가이드 제공까지**임을 고객에게 명확히 전달

### 4.2 `ai_optimization` — 02 AI 검색 최적화 (6.0h)

- [ ] **소개글 최적화** — ⚠️ CLAUDE.md 사실 지어내기 금지 원칙 직결(2026-07-08 `guide_generator.py` intro 프롬프트 사고 재발 방지 원칙과 동일 적용). 가격·영업시간·수용인원·시설(단체석·프라이빗룸 등)·운영기간·수상 이력을 **고객이 실제로 제공한 정보 없이 지어내지 말 것**. 확인 안 된 항목은 생략하거나 "스마트플레이스에서 확인하세요" 문구로 대체.
- [ ] **톡톡 채팅방 메뉴 최적화** — 명칭은 반드시 "톡톡 채팅방 메뉴"(구 "톡톡 FAQ" 사용 금지, 2024.02.14 네이버 개편 반영). `GET /api/tools/talktalk-templates/{category}`(25개 업종×5종 정적 템플릿) 활용 가능, 그대로 복붙보다 사업장 이름·시그니처 메뉴로 치환할 것
- [ ] **후기 답글 10건 작성** — `GET /api/tools/reply-templates/{category}/{sentiment}`(업종×감정×패턴) 활용 가능. 템플릿 문구를 동일하게 반복 사용하면 스팸처럼 보이므로 건별로 일부 표현 변경 권장
- [ ] 핵심 키워드 보강 — `keyword_gap` 결과 있으면 우선순위 참고(business_id 스캔 이력 있는 경우)

### 4.3 `comprehensive` — 03 종합 풀패키지 (11.2h)

- [ ] §4.1 체크리스트 전체
- [ ] §4.2 체크리스트 전체
- [ ] 1:1 코칭 세션 — 별도 예약(카카오톡), 자동 예약 시스템 없음
- [ ] 완료 후 30일 재진단 — `work_completed_at` 기록 시 스케줄러가 자동 트리거(§6 참조), 운영자가 별도로 잡을 필요 없음

---

## 5. 완료 처리 단계

⚠️ **알려진 불일치**: `/admin/delivery/{id}` 완료 처리 모달의 `COMPLETION_CHECKLIST`(`AdminDeliveryDetailClient.tsx:35-40`)는 패키지 구분 없이 고정 4항목("소개글 등록 완료" / "키워드 최적화 완료" / "톡톡 채팅방 메뉴 등록 완료" / "후기 답글 템플릿 제공")이다. 이 항목들은 사실상 **02 패키지 기준**이라, **01 주문 완료 처리 시 화면 체크박스를 그대로 믿지 말고 §4.1 체크리스트로 실제 완료 여부를 판단**할 것. (코드 자체를 패키지별 분기로 고칠지는 별도 결정 사항 — 필요하면 이어서 처리 가능)

- `completion_report.summary`에 특이사항·다음 단계 안내 기록 (자유 텍스트, 시스템이 강제하는 구조 없음)
- 스크린샷 등 납품 자료는 `/admin/delivery/{id}` → 파일 URL 등록(Supabase Storage 서명 URL 권장, `materials_url` JSONB 배열에 누적)
- "완료 처리" 버튼은 `completion_report` 등록과 `status=completed` 전환이 한 번에 일어남 — 되돌릴 수 없으므로 자료 등록 후 마지막에 클릭

---

## 6. 완료 후 처리

- [ ] **부운영자 권한 해제 요청** — ⚠️ 자동 해제 기능 없음(코드 전체 확인 완료, `delivery.py`·`jobs.py` 어디에도 해제 로직 없음). 완료 메시지에 "부운영자 권한 해제해 주세요" 안내를 **매번 수동으로** 포함할 것. 잊으면 고객 스마트플레이스에 운영자 접근 권한이 무기한 남는 보안·신뢰 리스크.
- [ ] 후기 요청 — 고객이 `POST /orders/{id}/testimonial` 제출 시 운영자에게 이메일 알림 자동 발송("코칭 쿠폰 발송 필요"). **카카오톡 쿠폰 발송은 여전히 수동** — 알림 받으면 바로 처리
- [ ] 30일 재진단(패키지 03만) — `work_completed_at + 30일` 조건으로 `delivery_30day_rescan_job`이 자동 실행. 운영자 개입 불필요, 결과만 사후 확인

---

## 7. 재작업(rework) / 환불 원칙

- `rework_count` 최대 2회 권장 표시(강제 차단 아님) — 3회 이상 요청 시 별도 상담 권장
- 환불은 **owner 전용**(`require_owner`), 결제완료 상태(`paid`/`in_progress`/`rework`)에서만 가능, 토스 실제 결제취소 API 호출 → 이중 클릭해도 락으로 중복 환불 방지됨(2026-07-11 수정 완료)
- 결제 전 주문은 "취소"만 가능(금전 이동 없음)

---

## 8. 활용 가능 사내 도구 (AI 호출 0, 무료)

⚠️ **API로 직접 호출하지 말 것** — 아래 두 엔드포인트는 프론트엔드에 연결된 화면이 없고(코드 전수 확인, 사용처 0건), `X-Admin-Key`가 아니라 **Basic+ 유료 사용자 로그인 토큰**이 있어야만 호출됨(`tools.py:27-41 _require_basic_plus`). 운영자가 실제로 API를 부를 방법이 없다 — **파일을 직접 열어 복사해 쓸 것**.

| 항목 | 실무 사용법 |
|------|-----------|
| 메뉴 일괄 등록 엑셀 | `GET /api/tools/menu-template.xlsx` — 이건 유일하게 프론트(`GuideClient.tsx`)에 붙어 있어 **고객 계정으로 로그인해 실제로 다운로드 가능**. |
| 톡톡 채팅방 메뉴 템플릿 | `backend/services/talktalk_templates.py`를 텍스트 에디터로 열기 → `TALKTALK_TEMPLATES["업종코드"]` 항목 찾기 → 5종(`reservation`/`menu_recommendation`/`parking`/`event`/`general`) 텍스트 복사 → `{business_name}`을 실제 상호명으로, `{signature_menu}`를 대표 메뉴로 직접 치환(전체 바꾸기)해서 사용 |
| 후기 답글 템플릿 | `backend/services/reply_templates.py`를 열기 → 감정별(`positive`/`neutral`/`negative`) 패턴 텍스트 복사 → `{business_name}` 치환 후 사용. 미등록 업종은 `POSITIVE_PATTERNS` 공통 패턴 사용 |

---

## 9. 최초 실제 건 진행 시 반드시 채울 항목 (현재 미검증 — 지어내지 않고 비워둠)

- [ ] **[결정 보류]** 네이버 스마트플레이스 **부운영자 초대 정확한 UI 경로**(메뉴명·화면 캡처 포함) — WebSearch 4회 시도(2026-07-17) 후에도 공식 문서 확인 안 됨. 채우는 방법 3안(§0-1 참조) 중 아직 미선택 — 다음 세션에서 방식부터 정한 뒤 진행할 것
- [ ] 신규 등록 심사 소요시간 실측치 (공개 자료 기준 "최소 2시간~최대 5일"이나 AEOlab 대행 케이스 실측 없음)
- [ ] 예상 작업시간(5.2h/6.0h/11.2h)이 실제와 맞는지 — 첫 3~5건 실측 후 §1 표 갱신
- [ ] 완료 후 부운영자 해제 요청 → 고객 응답률/누락 빈도 (안 지켜지면 시스템 알림 자동화 검토)

시범 5건 완료 후 이 문서를 정식 SOP로 승격(`docs/agency_service_remaining_tasks_v1.0.md §6 Sprint 5` 원 계획과 합류).

---

## 10. 관련 문서

- `docs/agency_service_and_iboss_improvements_v1.0.md` — 최초 기획(가격·범위 결정 이력)
- `docs/agency_pricing_strategy_v1.0.md` — 2026-07-15 가격 갱신(59,000/79,000/129,000원 현재가)
- `docs/agency_service_remaining_tasks_v1.0.md` — Sprint 진행 이력, §6이 SOP 항목 원안
- `docs/kakao_delivery_templates_manual_v1.0.md` — 카카오 알림톡 템플릿 신청 매뉴얼(별도)
- `docs/naver_gpt_work_standard_v1.0.md` — 키워드/업종 분류 작업 시 필수 참조
