// ScoreEvidenceCard.tsx — 점수 근거 카드 (v3.1 6항목 / v3.0 graceful fallback)
// dashboard/ScoreEvidenceCard.tsx (app router local copy)
// components/dashboard/ScoreEvidenceCard.tsx 와 동일 버전 유지 (2개 위치 존재)

export { default } from "@/components/dashboard/ScoreEvidenceCard";
